# Layer 2 — Configuration Samples

This folder contains sample MongoDB documents for the Export / Extract Configuration layer.

## Contents

| File | Description |
|---|---|
| `report-definition.json` | Sample report definition object |
| `parameter-schema.json` | Sample parameter schema for a report |
| `delivery-profile.json` | Sample delivery profile configuration |
| `file-naming-rule.json` | Sample file naming rule with tokens |
| `security-access-role.json` | Sample security and access role definition |

> These are representative samples used for planning and documentation purposes.
> Add real samples as they are created by the Ops config process.
